CAPEXA Consulting — static website
Upload the whole folder to the web root. Entry points: /en/index.html and /de/index.html (root index.html redirects by browser language).

TODO before go-live:
 - Domain is set to https://www.capexa.de. If the site is served without "www", search & replace the domain in all files.
 - Complete the TODO items in the JSON-LD (legalName, address, VAT ID, register) and the [bracketed] values in imprint.html and privacy.html once the company is founded.
